@model IEnumerable<CompanyContact.Models.ContactPerson>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.FirstName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.LasttName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Email)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.PhoneNumver)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LasttName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PhoneNumver)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ContactPersonId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ContactPersonId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ContactPersonId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
